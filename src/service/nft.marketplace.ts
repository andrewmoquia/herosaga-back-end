export const sellNFTOnMarketplace = (req: any, res: any) => {
   console.log(req, res)
   //Create transaction record
   //Update status for sale of nft
}

export const buyNFTOnMarketplace = (req: any, res: any) => {
   console.log(req, res)
   //Look for transaction record by nftid
   //Buy if not on process or for sale
   //Deduct payment of buyer
   //Transfer payment to seller
   //Tranfer NFT
}
